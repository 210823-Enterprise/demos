const myLog = function(message) {
    console.log(message);
}

// Anonymous function above and below:
// Arrow functions weren't introduced UNTIL 2015 (ES6)
const myLog2 = (message) => {

    console.log(message);
}

const myLog3  = (message) => console.log(message);